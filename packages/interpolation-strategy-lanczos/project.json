{
  "name": "@cxing/interpolation-strategy-lanczos",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/interpolation-strategy-lanczos/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/interpolation-strategy-lanczos",
        "command": "tsc -p tsconfig.json"
      },
      "outputs": ["{projectRoot}/.dist"],
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/interpolation-strategy-lanczos",
        "command": "tsc --build tsconfig.json --emitDeclarationOnly"
      },
      "outputs": [
        "{projectRoot}/.dist/**/*.d.ts",
        "{projectRoot}/.dist/**/*.d.ts.map",
        "{projectRoot}/.dist/tsconfig.tsbuildinfo"
      ],
      "dependsOn": ["^typecheck"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/interpolation-strategy-lanczos",
        "command": "vitest run --passWithNoTests"
      }
    }
  },
  "tags": []
}
